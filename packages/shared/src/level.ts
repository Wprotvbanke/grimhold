import { type Aabb, boxFromCenter } from './math.js';

/**
 * Содержимое стартового города. Отдельный файл, потому что город собран
 * вручную, а дикие земли вокруг генерируются процедурно (см. chunks.ts).
 *
 * Один и тот же список используют сервер (коллизии) и клиент (рендер):
 * расхождение геометрии сразу ломает предсказание, поэтому источник один.
 */

export interface LevelBox {
  /**
   * Вид поверхности. От него зависит текстура на клиенте, поэтому город
   * (мостовая) и дикие земли (грунт) разделены: одинаковый пол под ногами
   * стирал бы разницу между безопасным местом и опасным.
   */
  kind:
    | 'floor'
    | 'ground'
    | 'wall'
    | 'pillar'
    | 'platform'
    | 'rock'
    | 'ruin'
    | 'timber'
    /** Фахверк: брус с побелкой. Верхние этажи построек. */
    | 'frame'
    /** Каменная кладка: цоколь, труба, очаг. */
    | 'brick'
    /** Половая доска. */
    | 'plank'
    /** Кровельная черепица. */
    | 'shingle'
    /** Окованный сундук подземелья. */
    | 'chest';
  box: Aabb;
  /**
   * Коробка ставит преграду, но не рисуется. Так стоит обстановка таверны:
   * столы и бочки показывает модель из пака, а телесность им даёт вот эта
   * невидимая коробка — иначе пришлось бы либо проходить сквозь стол, либо
   * видеть серый ящик поверх модели.
   */
  hidden?: boolean;
  /**
   * Обратное `hidden`: коробка рисуется, но пути не преграждает.
   *
   * Нужна для тонких настилов и порогов. Шага через препятствие в движении
   * нет — останавливает любая коробка, какой бы низкой ни была, — и дощатый
   * пол таверны в шесть сантиметров запирал дверь наглухо.
   */
  noCollide?: boolean;
}

/**
 * Сторона города, м.
 *
 * Было 60 — город помещался в один чанк, но таверна, ратуша и дома с улицами
 * в него не влезали даже тесно. Выросли до 120 — стало просторно до пустоты,
 * и владелец ужал до **84**: улицы к воротам вдвое короче, кварталы те же.
 *
 * Город всё равно шире своего чанка (64 м): его коробки живут в чанке (0, 0),
 * а столкновения берутся из девяти чанков вокруг игрока, так что стена
 * на 42-м метре видна и телесна из соседнего чанка тоже. Соседние чанки
 * диких земель при этом обязаны уступать место городу — см. `insideTown`
 * и `TOWN_CLEARANCE`.
 *
 * Меняя это число, проверяй **планом сверху**: на 84 в новой стене оказались
 * фонари у ворот и два дома по краям кварталов, их пришлось отодвинуть.
 */
export const TOWN_SIZE = 84;
const HALF = TOWN_SIZE / 2;

/**
 * Сколько метров за стеной дикие земли держат пустыми.
 *
 * Камень, дерево или нода, выросшие вплотную к стене, торчали бы из неё
 * и загораживали ворота; вышки к тому же выступают за линию стены.
 */
export const TOWN_CLEARANCE = 8;

/** Внутри ли точка города — с запасом `margin` за стены. */
export function insideTown(x: number, z: number, margin = 0): boolean {
  return Math.abs(x) <= HALF + margin && Math.abs(z) <= HALF + margin;
}

/**
 * Городские стены — модели `walls.glb` (вышка, пролёт стены, арка с воротами).
 *
 * Размеры здесь — после масштаба, в метрах: по ним и коробки столкновений,
 * и расстановка моделей на клиенте (houses.ts). Масштаб у каждой части свой:
 * вышка и арка крупнее исходника, чтобы угол и въезд читались издалека,
 * а проход в арке — четыре метра, не три: исходные ворота были тесны даже
 * для игрока, не то что для двоих рядом.
 *
 * **Размеры — по телу, а не по габариту.** Зубцы и карниз наверху шире кладки:
 * у вышки тело 3.22 м при габарите 3.79, у арки 7.76 при 7.93. Стыковали
 * сначала по габариту — и пролёты упирались в воздух в трети метра от вышки,
 * владелец увидел щели в каждом углу.
 */
export const TOWN_WALLS = {
  tower: { scale: 1.2, size: 3.85, height: 7.7 },
  arch: { scale: 1.3, width: 10.1, depth: 2, height: 5.5, opening: 4, lintel: 3.9 },
  /** `piece` — длина пролёта в модели; на стене пролёты растягиваются ровно по промежутку. */
  wall: { piece: 12, thickness: 1, height: 4.1 },
} as const;

export interface WallPiece {
  kind: 'tower' | 'wall' | 'arch';
  x: number;
  z: number;
  /**
   * Разворот как у `rotation.y` в three. Локальная X модели идёт вдоль стены,
   * локальная −Z смотрит внутрь города.
   */
  turn: number;
  /** Длина вдоль стены, м. */
  length: number;
}

/**
 * Раскладка стен: вышки по углам, арка с воротами посреди каждой стороны,
 * между ними пролёты.
 *
 * Одна функция на клиент и сервер: по ней и модели, и коробки. Разъедься
 * они — игрок упирался бы в воздух у стены или проходил сквозь вышку.
 */
export function townWallLayout(): WallPiece[] {
  const { tower, arch, wall } = TOWN_WALLS;
  const pieces: WallPiece[] = [];

  for (const sx of [-1, 1]) {
    for (const sz of [-1, 1]) {
      pieces.push({ kind: 'tower', x: sx * HALF, z: sz * HALF, turn: 0, length: tower.size });
    }
  }

  // Разворот такой, чтобы локальная −Z смотрела внутрь: x' = x·cos + z·sin,
  // z' = −x·sin + z·cos (как `rotation.y` в three).
  const sides = [
    { along: 'x', at: -HALF, turn: Math.PI },
    { along: 'x', at: HALF, turn: 0 },
    { along: 'z', at: -HALF, turn: -Math.PI / 2 },
    { along: 'z', at: HALF, turn: Math.PI / 2 },
  ] as const;

  for (const side of sides) {
    const at = (along: number): { x: number; z: number } =>
      side.along === 'x' ? { x: along, z: side.at } : { x: side.at, z: along };

    pieces.push({ kind: 'arch', ...at(0), turn: side.turn, length: arch.width });

    const from = arch.width / 2;
    const to = HALF - tower.size / 2;
    const count = Math.ceil((to - from) / wall.piece);
    const length = (to - from) / count;
    for (const sign of [-1, 1]) {
      for (let i = 0; i < count; i++) {
        pieces.push({ kind: 'wall', ...at(sign * (from + length * (i + 0.5))), turn: side.turn, length });
      }
    }
  }

  return pieces;
}

/**
 * Коробки стен. Вид дают модели, поэтому все невидимые.
 *
 * Арка — не одна коробка, а две опоры и перемычка над проходом: иначе ворота
 * были бы глухими.
 */
function townWallBoxes(): LevelBox[] {
  const { tower, arch, wall } = TOWN_WALLS;
  const boxes: LevelBox[] = [];

  for (const piece of townWallLayout()) {
    const alongX = Math.abs(Math.cos(piece.turn)) > 0.5;
    /** Коробка на стене: `offset` вдоль стены от середины части. */
    const onWall = (offset: number, length: number, depth: number, fromY: number, toY: number): LevelBox => ({
      kind: 'wall',
      hidden: true,
      box: boxFromCenter(
        alongX ? piece.x + offset : piece.x,
        (fromY + toY) / 2,
        alongX ? piece.z : piece.z + offset,
        alongX ? length : depth,
        toY - fromY,
        alongX ? depth : length,
      ),
    });

    if (piece.kind === 'tower') {
      boxes.push({
        kind: 'wall',
        hidden: true,
        box: boxFromCenter(piece.x, tower.height / 2, piece.z, tower.size, tower.height, tower.size),
      });
    } else if (piece.kind === 'wall') {
      boxes.push(onWall(0, piece.length, wall.thickness, 0, wall.height));
    } else {
      const post = (arch.width - arch.opening) / 2;
      const shift = arch.opening / 2 + post / 2;
      boxes.push(
        onWall(-shift, post, arch.depth, 0, arch.height),
        onWall(shift, post, arch.depth, 0, arch.height),
        onWall(0, arch.opening, arch.depth, arch.lintel, arch.height),
      );
    }
  }

  return boxes;
}


/**
 * Фонари города.
 *
 * Раскладка лежит здесь, а не рядом с моделями, по той же причине, что и
 * здания площади: **в столб упираются**. Разъедься список с картинкой — игрок
 * упирался бы в пустоту или проходил сквозь фонарь.
 *
 * Ни один не стоит на оси ворот: через них ходят, а столб посреди прохода —
 * это стена. Один такой однажды оказался вмурован в стену рынка, и заметили
 * это только когда стали считать столкновения.
 */
export const TOWN_LAMPS: readonly { x: number; z: number }[] = [
  // По бокам северных и южных ворот, а не в самом проёме: у опор арки,
  // в четырёх с половиной метрах внутрь от стены.
  { x: 6.8, z: 37.5 },
  { x: -6.8, z: 37.5 },
  { x: 6.8, z: -37.5 },
  { x: -6.8, z: -37.5 },
  // Вдоль улиц — у их края, а не на полосе главных улиц (|x| и |z| ≤ 4.5):
  // когда город вырос и встали дома, четыре фонаря оказались посреди улицы
  // и были отодвинуты к краю.
  { x: -6, z: 6 },
  { x: 9, z: 6 },
  { x: -6, z: -5.8 },
  { x: 10, z: -5.8 },
  { x: -18, z: 5.8 },
  // Стоял в (18, 10) — ближайший к церкви; владелец попросил перенести его
  // к ней. Теперь у фасада, по левую руку от двери, у края южной улицы.
  { x: 5.3, z: 13.9 },
  { x: -24, z: -6 },
  { x: 20, z: -20 },
  // На площади — у края южной улицы, в стороне от маршрута Громи.
  { x: -5.8, z: 12 },
  // Стоял у двери таверны. Таверну снесли под другое здание, а угол улицы
  // без огня остался бы тёмным — фонарь на прежнем месте.
  { x: -11.4, z: -7.5 },
];

/** Столб фонаря: тонкий, но телесный. */
const LAMP_RADIUS = 0.18;
/** Высота столба. Клиент приводит модель ровно к ней. */
export const LAMP_HEIGHT = 3.4;
function lightBoxes(): LevelBox[] {
  const boxes: LevelBox[] = [];

  for (const lamp of TOWN_LAMPS) {
    boxes.push({
      kind: 'pillar',
      hidden: true,
      box: boxFromCenter(lamp.x, LAMP_HEIGHT / 2, lamp.z, LAMP_RADIUS * 2, LAMP_HEIGHT, LAMP_RADIUS * 2),
    });
  }

  return boxes;
}

/**
 * Городская казна — единственное безопасное хранилище в игре.
 *
 * Стоит на площади, у всех на виду и близко к точке входа: банк нужен чаще
 * всего сразу после возвращения из диких земель, и заставлять искать его по
 * городу значило бы наказывать за осторожность.
 *
 * Хранилище общее на аккаунт, а не на персонажа: вещи кладёт тот, кто дошёл,
 * а забирает любой свой — так дворф-кузнец снабжает эльфа-травника, не
 * встречаясь с ним в мире.
 */
export const BANK = {
  /**
   * Середина площади: владелец перенёс казну с краю в центр города
   * и сделал втрое больше, а сверху поставил памятник: теперь это не ларец
   * у дороги, а постамент посреди города.
   */
  x: -0.3,
  z: -2.5,
  /**
   * С какого расстояния открывается сундук — считается от середины.
   *
   * Рос вместе с ларцом: три метра от середины теперь внутри самого
   * сундука — до такой точки не дойти, и казна не открывалась бы вовсе.
   */
  range: 6,
  /**
   * Во сколько раз ларец больше модели. По нему же считаны габариты ниже.
   */
  scale: 3,
  /**
   * Габариты — по модели каменного ларца (`bank.glb`, ставит houses.ts):
   * модель длинной стороной вдоль X, увеличена в 1.1 раза при сборке и втрое
   * в игре. По ним же и коробка столкновений, и прицел «открыть казну»,
   * и высота, на которой стоит памятник.
   */
  width: 6.6,
  depth: 3.81,
  height: 2.25,
} as const;

/**
 * Памятник на казне: ангел посреди крышки.
 *
 * Стоит ровно над серединой ларца, поэтому своих координат у него нет:
 * двинется казна — двинется и памятник. Телесности у него нет:
 * он выше роста и стоит на коробке казны, в него не врезаться.
 */
export const BANK_STATUE = {
  /** Рост памятника в метрах — задан при сборке модели. */
  height: 4,
} as const;

/**
 * Спуск в подземелье — люк на городской площади.
 *
 * Стоит в городе намеренно: вход всегда доступен, и бесплатный стартовый набор
 * делает его доступным даже тому, кто всё потерял. Искать вход по диким землям
 * значило бы наказывать за неудачу прошлого забега.
 */
export const DUNGEON_GATE = {
  /*
   * Люк стоял посреди площади у казны. Владелец перенёс его в северо-западный
   * квартал — в проход между лавкой на колёсах и домом, за лавкой: вход
   * в подземелье не на виду у всего города. Вид даёт модель `trapdoor.glb`
   * (ставит houses.ts), телесности у люка нет — через раму не шагать.
   */
  x: -35.2,
  z: -9.5,
  /** С какого расстояния открывается спуск. */
  range: 3,
  width: 2.6,
  depth: 2.6,
  height: 0.6,
} as const;

/**
 * Здания площади — ратуша и городской дом, модели от владельца.
 *
 * Встали вместо двух голых стен, что делили площадь: стены были «есть за чем
 * прятаться», здания дают то же укрытие, но выглядят городом, а не полигоном.
 *
 * Дом стоит **вдоль восточной стороны северной улицы**, ратуша («церковь»)
 * перенесена владельцем к юго-востоку от точки появления; обе фасадом с дверью
 * на запад — к улице и к площади. Первым их
 * поставили к северу от точки появления фасадом на площадь — и ратуша
 * перегородила ось северных ворот: из города было не выйти, упал тест
 * движения. Ось ворот держим пустой, как и для фонарей.
 *
 * `footprint` — пятно здания в его собственных координатах, по габаритам
 * модели и чуть внутрь: крыльцо и свесы кровли не должны останавливать
 * за полметра до стены. Внутрь не войти — это коробка, а не таверна.
 * `turn` — разворот вокруг вертикали, в радианах, как у `rotation.y` в three:
 * фасад модели смотрит в +Z, −π/2 разворачивает его на −X, к улице.
 *
 * Соседи: фонарь в (10, −2) южнее, платформы с x = 12 восточнее, дорога
 * сквозных проверок к люку идёт по z = −2 — всё снаружи.
 */
/**
 * Пятна домов у земли в их координатах — по моделям, чуть внутрь.
 *
 * Числа сняты с собранных моделей (`village_house*.glb`) и ужаты на три
 * десятых: свес кровли и крыльцо не должны останавливать за полметра
 * до стены. Внутрь домов не войти — это коробки, а не таверна.
 */
const HOUSE = {
  /** Каменный с мансардой и трубой, 9 м. */
  one: { minX: -4.6, maxX: 4.6, minZ: -4.1, maxZ: 4.1 },
  /** Широкий с навесом на столбах, 9 м. Самый глубокий из всех. */
  two: { minX: -6.1, maxX: 6.1, minZ: -7.9, maxZ: 7.9 },
  /** Высокий с башенкой, 11 м — доминанта квартала. */
  three: { minX: -4.5, maxX: 4.5, minZ: -5.5, maxZ: 5.5 },
  /** Узкий двухэтажный с большими окнами, 9 м. */
  four: { minX: -3.6, maxX: 3.6, minZ: -3.2, maxZ: 3.2 },
  /** Двор с крытой галереей, 9 м. */
  five: { minX: -6.1, maxX: 6.1, minZ: -6.0, maxZ: 6.0 },
  /** Фахверковый амбар с воротами, 7 м. */
  six: { minX: -4.7, maxX: 4.7, minZ: -4.9, maxZ: 4.9 },
  /** Приземистый каменный, 7 м — самый широкий. */
  seven: { minX: -6.9, maxX: 6.9, minZ: -5.2, maxZ: 5.2 },
  /** Двухэтажный с галереей, 8 м. */
  eight: { minX: -3.6, maxX: 3.6, minZ: -3.6, maxZ: 3.6 },
  /** Лавка на колёсах у люка — сюжетная, осталась от прежней застройки. */
  wagon: { minX: -1.2, maxX: 1.2, minZ: -2.6, maxZ: 2.3 },
} as const;

/**
 * На сколько утоплены деревенские дома.
 *
 * Сборка ставит дом основанием на ноль по габаритам модели, но у этих
 * в габариты попадает свес кровли или порог — и корпус оказывается на
 * палец выше мостовой. На кадре с трёх метров это видно: дом будто
 * не достаёт до земли. Общая осадка для всех, кроме приземистого.
 */
const VILLAGE_SINK = 0.12;

/**
 * На сколько утоплен приземистый каменный дом (`village_house7`).
 *
 * Сборка ставит дом основанием на ноль по габаритам, но у этого внизу
 * широкий цоколь, и на ровной мостовой он читается зависшим — владелец
 * заметил это у обеих его копий. Число одно на всю модель: разъедься они,
 * один дом сидел бы в земле глубже другого.
 */
const SEVEN_SINK = 0.6;

export const TOWN_HOUSES: readonly {
  model:
    | 'village_house1'
    | 'village_house2'
    | 'village_house3'
    | 'village_house4'
    | 'village_house5'
    | 'village_house6'
    | 'village_house7'
    | 'village_house8'
    | 'wagon';
  x: number;
  z: number;
  turn: number;
  /** Во сколько раз модель больше собранной; пятно и высота даны уже с ним. */
  scale?: number;
  /**
   * На сколько метров опустить модель в землю.
   *
   * Сборка ставит дом основанием на ноль по габаритам, а у иного дома внизу
   * цоколь или выступ, и на ровной мостовой он читается зависшим. Опускаем
   * вид — телесность при этом не трогаем: коробка и так от земли.
   */
  sink?: number;
  height: number;
  footprint: { minX: number; maxX: number; minZ: number; maxZ: number };
}[] = [
  /*
   * Кварталы. Главные улицы к воротам — полосы |x| ≤ 4.5 и |z| ≤ 4.5 — пустые,
   * площадь вокруг казны и точки появления тоже. Дома встают рядами вдоль улиц
   * фасадом к ним и уходят вглубь к стенам, через три-четыре метра друг от друга.
   * Разворот: 0 — фасад на юг (+Z), π — на север, π/2 — на восток, −π/2 — на запад.
   *
   * Застройка сменилась целиком: старые дома владелец убрал, оставив только
   * таверну и лавку на колёсах у люка. Новые — из деревенского пака, восемь
   * видов; повторяются они намеренно, как повторяются дома в настоящем
   * городке, но соседями ставятся разные.
   */

  // Северо-восток: вдоль северной улицы, фасадом на запад, к ней.
  { model: 'village_house3', x: 11.5, z: -14, turn: -Math.PI / 2, height: 11, sink: VILLAGE_SINK, footprint: HOUSE.three },
  { model: 'village_house1', x: 11, z: -27, turn: -Math.PI / 2, height: 9, sink: VILLAGE_SINK, footprint: HOUSE.one },
  // Стоял в (12.5, −36.5), почти вплотную к северной стене — владелец
  // отодвинул его в глубину квартала, к востоку.
  { model: 'village_house7', x: 30.6, z: -28.4, turn: -Math.PI / 2, height: 7, footprint: HOUSE.seven, sink: SEVEN_SINK },
  // Северо-восток: вдоль восточной улицы, фасадом на юг.
  { model: 'village_house6', x: 26, z: -11, turn: 0, height: 7, sink: VILLAGE_SINK, footprint: HOUSE.six },

  // Юго-восток: вдоль восточной улицы, фасадом на север.
  { model: 'village_house4', x: 25, z: 10.5, turn: Math.PI, height: 9, sink: VILLAGE_SINK, footprint: HOUSE.four },
  // Юго-восток: вдоль южной улицы, фасадом на запад.
  { model: 'village_house8', x: 11, z: 15, turn: -Math.PI / 2, height: 8, sink: VILLAGE_SINK, footprint: HOUSE.eight },
  { model: 'village_house5', x: 13, z: 29, turn: -Math.PI / 2, height: 9, sink: VILLAGE_SINK, footprint: HOUSE.five },

  // Юго-запад: вдоль западной улицы, фасадом на север.
  { model: 'village_house2', x: -27, z: 13, turn: Math.PI, height: 9, sink: VILLAGE_SINK, footprint: HOUSE.two },
  /*
   * Юго-запад: вдоль южной улицы, фасадом на восток.
   *
   * Здесь стоял приземистый каменный (`village_house7`) — он парил над
   * мостовой, и владелец поменял его местами с узким двухэтажным из
   * северо-западного квартала.
   */
  { model: 'village_house4', x: -13, z: 14, turn: Math.PI / 2, height: 9, sink: VILLAGE_SINK, footprint: HOUSE.four },
  { model: 'village_house1', x: -11.5, z: 28, turn: Math.PI / 2, height: 9, sink: VILLAGE_SINK, footprint: HOUSE.one },

  // Северо-запад, за таверной: вдоль северной улицы, фасадом на восток.
  { model: 'village_house6', x: -11.5, z: -30, turn: Math.PI / 2, height: 7, sink: VILLAGE_SINK, footprint: HOUSE.six },
  // Северо-запад, в глубине квартала за люком. Приземистый каменный утоплен
  // в мостовую: собранный по габаритам, он стоял на ней, будто завис.
  { model: 'village_house7', x: -29, z: -29, turn: 0, height: 7, footprint: HOUSE.seven, sink: SEVEN_SINK },

  // Лавка на колёсах у люка — сюжетная, стоит там же, где стояла.
  { model: 'wagon', x: -30, z: -8, turn: Math.PI / 2, height: 3.6, footprint: HOUSE.wagon },
];

/**
 * Таверна — модель `tavern.glb` с настоящим залом внутри.
 *
 * Здание Г-образное: корпус и крыло на северо-востоке, в углу между ними
 * крыльцо, и с крыльца — дверь в восточной стене корпуса. Стоит в
 * северо-западном квартале, дверью к площади (разворот 0: восток модели —
 * восток мира), рядом с фонарём, что светил и старой таверне.
 *
 * Внутрь входят, поэтому одной коробкой, как дом, её не сделать: стены —
 * отдельными коробками по плоскостям модели, проход оставлен **только
 * в двери**. Плоскости сняты с треугольников первого этажа — толщина стен
 * 20–25 см, проём шириной в метр.
 */
export const TAVERN = {
  x: -16,
  z: -14,
  /**
   * Пол зала в модели поднят на 15 см, а шага через порог в движении нет:
   * игрок упёрся бы в пол у двери. Модель опущена — цоколь уходит в мостовую.
   */
  sink: 0.15,
  wallHeight: 3.7,
} as const;

type Rect = readonly [minX: number, maxX: number, minZ: number, maxZ: number];

/** Стены таверны в её координатах. Проём двери — z 3.11…4.11 в восточной стене. */
const TAVERN_WALLS: readonly Rect[] = [
  [-4.25, -4.0, -5.57, 5.37], // западная
  [-4.25, 4.25, -5.57, -5.35], // северная
  [4.05, 4.25, -5.57, -0.1], // восточная стена крыла
  [2.05, 4.25, -0.3, -0.1], // южная стена крыла, смотрит на крыльцо
  [2.05, 2.25, -0.3, 3.11], // восточная стена корпуса до двери
  [2.05, 2.25, 4.11, 5.37], // и после двери
  [-4.25, 2.25, 5.15, 5.37], // южная
];

/** Зал: корпус и выступ в крыло. По нему музыка знает, что игрок внутри. */
export const TAVERN_HALL: readonly Rect[] = [
  [-4.0, 2.05, -5.35, 5.15],
  [2.05, 4.05, -5.35, -0.3],
];

/**
 * Телесность мебели: вид даёт `client/src/props.ts`, координаты общие.
 * Разъедься они — стол нарисован в одном месте, а упираешься в другое.
 */
const TAVERN_FURNITURE: readonly (readonly [minX: number, maxX: number, minZ: number, maxZ: number, height: number])[] = [
  [-4.0, 1.1, -5.35, -3.95, 1.1], // стойка и шкафы за ней
  // Столы — оба вдоль западной стены. Второй стоял прямо у двери и мешал
  // войти: владелец попросил поставить его рядом с первым.
  [-3.0, -1.8, -0.4, 2.0, 0.78],
  [-3.0, -1.8, 2.4, 4.8, 0.78],
  // Барная стойка из ящиков в крыле — там, где стояла бочка. За ней проход
  // в метр: зайти с севера и стоять, как за стойкой.
  [2.15, 2.9, -3.4, -0.3, 1.0],
  [2.15, 2.9, -5.35, -4.45, 1.6], // ящики стопкой в северном углу крыла
  [3.2, 4.05, -5.35, -4.45, 1.0], // бочка в северо-восточном углу
  [0.6, 1.9, 4.45, 5.15, 1.0], // припасы у южной стены, в стороне от двери
];

/** Внутри ли зала таверны — для музыки. */
export function insideTavern(x: number, z: number): boolean {
  const localX = x - TAVERN.x;
  const localZ = z - TAVERN.z;
  return TAVERN_HALL.some(([minX, maxX, minZ, maxZ]) => localX >= minX && localX <= maxX && localZ >= minZ && localZ <= maxZ);
}

function tavernBoxes(): LevelBox[] {
  const at = (rect: Rect, height: number, kind: LevelBox['kind']): LevelBox => ({
    kind,
    hidden: true,
    box: {
      minX: TAVERN.x + rect[0],
      maxX: TAVERN.x + rect[1],
      minY: 0,
      maxY: height,
      minZ: TAVERN.z + rect[2],
      maxZ: TAVERN.z + rect[3],
    },
  });
  return [
    ...TAVERN_WALLS.map((rect) => at(rect, TAVERN.wallHeight, 'wall')),
    ...TAVERN_FURNITURE.map(([minX, maxX, minZ, maxZ, height]) => at([minX, maxX, minZ, maxZ], height, 'timber')),
  ];
}

/**
 * Разворот, при котором фасад модели (+Z) смотрит из точки на цель.
 * Тот же угол, что у `rotation.y` в three.
 */
function facing(fromX: number, fromZ: number, toX: number, toZ: number): number {
  return Math.atan2(toX - fromX, toZ - fromZ);
}

/**
 * Размер клетки, которой обводится пятно здания, стоящего под углом к осям.
 *
 * Коробки у нас выровнены по осям, а здание может стоять под любым углом.
 * Одна коробка вокруг повёрнутого пятна раздувается: ратуша под 34° к осям
 * давала коробку 11×11 м вместо 7×8.5, и у углов стояли невидимые стены
 * в полтора метра.
 *
 * Сначала пятно резали на полосы поперёк глубины. Полоса тянется во всю
 * ширину здания, и её коробка раздувается вдоль фасада тем сильнее, чем
 * здание шире: у вдвое увеличенной церкви фонарь в двух метрах перед
 * фасадом оказался внутри невидимой стены. Клетка раздувается одинаково
 * по обе стороны и не больше, чем на треть своего размера.
 *
 * Клетки ставятся **только по краю** пятна: сквозь край внутрь не попасть,
 * и внутренние коробки только множили бы проверки. Здание вдоль осей — одна
 * коробка, ровно по пятну.
 */
const HOUSE_CELL = 1.5;

/**
 * Коробки здания в мире: пятно поворачивается вместе с моделью.
 *
 * Поворот тот же, что у `rotation.y` в three: x' = x·cos + z·sin,
 * z' = −x·sin + z·cos. Разверни одно без другого — и игрок упирается
 * в воздух сбоку от здания, а сквозь фасад проходит.
 */
function houseBoxes(): LevelBox[] {
  const boxes: LevelBox[] = [];
  for (const { x, z, turn, height, footprint } of TOWN_HOUSES) {
    const cos = Math.cos(turn);
    const sin = Math.sin(turn);
    const width = footprint.maxX - footprint.minX;
    const depth = footprint.maxZ - footprint.minZ;
    const aligned = Math.abs(sin) < 1e-6 || Math.abs(cos) < 1e-6;
    const columns = aligned ? 1 : Math.ceil(width / HOUSE_CELL);
    const rows = aligned ? 1 : Math.ceil(depth / HOUSE_CELL);
    const stepX = width / columns;
    const stepZ = depth / rows;

    for (let column = 0; column < columns; column++) {
      for (let row = 0; row < rows; row++) {
        const edge = column === 0 || row === 0 || column === columns - 1 || row === rows - 1;
        if (!edge) continue;
        const fromX = footprint.minX + stepX * column;
        const fromZ = footprint.minZ + stepZ * row;
        const corners = [
          [fromX, fromZ],
          [fromX, fromZ + stepZ],
          [fromX + stepX, fromZ],
          [fromX + stepX, fromZ + stepZ],
        ].map(([cx, cz]) => ({ x: x + cx! * cos + cz! * sin, z: z - cx! * sin + cz! * cos }));
        boxes.push({
          kind: 'wall',
          hidden: true,
          box: {
            minX: Math.min(...corners.map((corner) => corner.x)),
            maxX: Math.max(...corners.map((corner) => corner.x)),
            minY: 0,
            maxY: height,
            minZ: Math.min(...corners.map((corner) => corner.z)),
            maxZ: Math.max(...corners.map((corner) => corner.z)),
          },
        });
      }
    }
  }
  return boxes;
}

export const TOWN_BOXES: readonly LevelBox[] = [
  // Мостовая города. Верх на y = 0.
  { kind: 'floor', box: boxFromCenter(0, -0.5, 0, TOWN_SIZE, 1, TOWN_SIZE) },

  // Городские стены: вышки по углам, арки с воротами на все четыре стороны.
  ...townWallBoxes(),

  // Здания площади: вид им дают модели, здесь только телесность.
  ...houseBoxes(),
  // Таверна: стены с проходом в двери и мебель зала.
  ...tavernBoxes(),



  // Фонари: вид им даёт клиент, здесь только телесность.
  ...lightBoxes(),

  // Городская казна: каменный ларец на площади. Вид даёт модель (houses.ts),
  // здесь только телесность.
  {
    kind: 'brick',
    hidden: true,
    box: boxFromCenter(BANK.x, BANK.height / 2, BANK.z, BANK.width, BANK.height, BANK.depth),
  },


  // Ступени с помостом у восточной стены убраны по просьбе владельца:
  // лестница из пола посреди улицы смотрелась поломкой, место ровное.
];

/**
 * Безопасная зона — город внутри стен.
 *
 * Здесь нельзя поднять руку на другого игрока: сервер отклоняет и удар,
 * и снаряд. Город — место, куда возвращаются с добычей, и торг у казны не
 * должен кончаться ножом в спину, пока человек разбирает рюкзак с отпущенным
 * курсором.
 *
 * Мобов это не касается: за стены их и так не пускают.
 *
 * Граница совпадает со стенами, а не проходит где-то рядом: игрок должен
 * видеть, где кончается защита. Проверяются **оба** участника — иначе
 * стрелять можно было бы из ворот по тем, кто внутри.
 */
export function isSafe(x: number, z: number): boolean {
  return Math.abs(x) <= HALF && Math.abs(z) <= HALF;
}

export const SPAWN_POINT = { x: 0, y: 0.1, z: 10 };

/**
 * Совместимость с ранним кодом и тестами: полный список коллизий города.
 * Игровой код должен ходить через ChunkedWorld, который знает и дикие земли.
 */
export const TEST_LEVEL: readonly LevelBox[] = TOWN_BOXES;
export const TEST_LEVEL_COLLIDERS: readonly Aabb[] = TOWN_BOXES.filter(
  (entry) => !entry.noCollide,
).map((entry) => entry.box);
